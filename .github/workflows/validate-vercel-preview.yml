name: validate-vercel-preview

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout exact merged main smoke harness
        uses: actions/checkout@v4
        with:
          ref: feb1cfaab15c44522c594d008a8b0190f400ff52
      - name: Verify preview release identity
        run: |
          python3 - <<'PY'
          import json, urllib.request
          url='https://spool-webmcp-qck0h1vn0-tejs-projects-70bb4568.vercel.app/release.json'
          expected='feb1cfaab15c44522c594d008a8b0190f400ff52'
          with urllib.request.urlopen(url, timeout=20) as r: release=json.load(r)
          assert release.get('commit') == expected, (release, expected)
          print('preview release identity verified', expected)
          PY
      - name: Install browser smoke dependency
        run: python3 -m pip install --disable-pip-version-check websocket-client
      - name: Run complete-product browser smoke against Vercel preview
        env:
          SPOOL_URL: https://spool-webmcp-qck0h1vn0-tejs-projects-70bb4568.vercel.app
          SPOOL_BROWSER: google-chrome
        run: python3 scripts/browser-smoke.py
